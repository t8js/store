{
  "name": "@t8/store",
  "version": "1.1.10",
  "description": "Vanilla JS/TS data container offering subscription to its updates",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "npx npm-run-all clean compile types",
    "clean": "node -e \"require('node:fs').rmSync('dist', { force: true, recursive: true });\"",
    "compile": "npx esbuild index.ts --bundle --outdir=dist --platform=neutral",
    "prepublishOnly": "npm run build",
    "preversion": "npx npm-run-all typecheck shape test",
    "shape": "npx codeshape",
    "test": "npx npm-run-all build test:compile test:run",
    "test:compile": "npx esbuild tests.ts --bundle --outdir=dist --platform=neutral",
    "test:run": "node dist/tests.js",
    "typecheck": "tsc --noEmit",
    "types": "tsc --emitDeclarationOnly"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/t8js/store.git"
  },
  "keywords": [
    "shared state",
    "store"
  ],
  "author": "axtk",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
